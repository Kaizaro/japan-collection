import {StyleSheet} from 'react-native';

/** @internal */
export const textStyles = StyleSheet.create({
    regular: {
        fontSize: 14,
        fontWeight: '300',
    },
    bold: {
        fontSize: 18,
        fontWeight: '700',
    },
});

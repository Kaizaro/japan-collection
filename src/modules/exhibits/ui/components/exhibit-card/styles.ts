import {StyleSheet} from 'react-native';

/** @internal */
export const exhibitCardStyles = StyleSheet.create({
    card: {
        marginVertical: 10,
        padding: 20,
        borderWidth: 1,
        borderRadius: 10,
    },
    image: {
        width: '100%',
    },
});

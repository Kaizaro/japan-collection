import {StyleSheet} from 'react-native';

import {scaleVertical} from '@shared/utils/scale';

export const exhibitWeaponSamuraiSubCategoriesStyles = StyleSheet.create({
    container: {
        width: '100%',
    },
    item: {
        marginVertical: scaleVertical(8),
    },
});
